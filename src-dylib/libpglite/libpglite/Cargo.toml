[package]
name = "libpglite"
edition = "2024"

[lib]
name = "pglite"
crate-type = ["cdylib"]

[dependencies]
const-hex = "1.19.1"
pglite = { path = "../pglite" }
postgres-types = { workspace = true }
